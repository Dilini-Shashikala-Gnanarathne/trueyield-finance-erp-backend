# =============================================================================
# Finance ERP — Docker Compose
#
# Services:
#   postgres             — PostgreSQL (payroll_db + finance_db + user_management_db)
#   kafka                — Apache Kafka 3.7 in KRaft mode (no Zookeeper)
#   user-service         — User management & identity microservice (REST :8085)
#   finance-service      — Finance microservice (REST :8082 / gRPC :9090)
#   payroll-service      — Payroll microservice (REST :8081) + Kafka producer
#   notification-service — Kafka consumer (payroll events → notifications)
#   payslip-service      — Payslip microservice (Kafka consumer :8084)
#   api-gateway          — Spring Cloud Gateway (:8080)
#
# Startup order: postgres → kafka → finance → payroll → notification → user → gateway
# =============================================================================

networks:
  finance-erp-net:
    driver: bridge

volumes:
  postgres-data:
    driver: local

services:

  auth-service:
    build:
      context: ./backend
      dockerfile: auth-service/Dockerfile

  marketplace-service:
    build:
      context: ./backend
      dockerfile: marketplace-service/Dockerfile

  finance-service:
    build:
      context: ./backend
      dockerfile: finance-service/Dockerfile

  payroll-service:
    build:
      context: ./backend
      dockerfile: payroll-service/Dockerfile

  notification-service:
    build:
      context: ./backend
      dockerfile: notification-service/Dockerfile

  payslip-service:
    build:
      context: ./backend
      dockerfile: payslip-service/Dockerfile

  api-gateway:
    build:
      context: ./backend
      dockerfile: api-gateway/Dockerfile
